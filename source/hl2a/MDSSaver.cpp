#include "MDSSaver.h"
//#include <mdslib.h>
//#include <mdsshr.h>
#include <stdio.h>

namespace HL2A
{
	bool MDSSaver::SaveData(char * keyParam, char * reserved)
	{
		int socket = -1;

		//socket = MdsConnect(_host);

		return true;
	}

	char * MDSSaver::Read(char * keyParam, char * reserved)
	{
		return NULL;
	}
}

